#include<stdio.h>

int main(){
    int a = 4;
    printf("The value of i is %d\n", ++a);
    printf("The value of i is %d\n", a);
    return 0;
}

 // >>>>>>> Important : Increment & Decrement <<<<<<<

    // 1. Increment:
    // i++ : It is like i = i + 1
    // i++ : If i is written first then it will first print i and then increment.

    // 2. Decrement:
    // i-- : It is like i = i - 1
    // 3. >> This program will print the value from 11 to 20.
    // ++i : If i is written first then it will first decrement i and then print.