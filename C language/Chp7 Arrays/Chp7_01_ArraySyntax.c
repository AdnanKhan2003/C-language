#include <stdio.h>

int main(){
    int marks[4];  // This tells compiler to allocate space for 4 integers.
    marks[0] = 23;
    marks[1] = 32;
    marks[2] = 43;
    marks[3] = 93;
    return 0;
}

// Arrays is a combination of same values. It is capable to store similar values.  
// Arrays is stored in contiguos block of memory.