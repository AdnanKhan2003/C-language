#include <stdio.h>
// Note: String is stored as an individual characters in memory.

int main(){
    // char *ptr = "Adnan bro is so pro that nobody have even thought of \n";
    // printf("%s", ptr);
    char st[] = "Adnan \n";
    printf("%s", st);
    return 0;
}